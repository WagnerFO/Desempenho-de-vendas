CREATE TABLE IF NOT EXISTS sor_outlets (
    Outlet_Identifier VARCHAR(10) PRIMARY KEY,
    Outlet_Establishment_Year INT,
    Outlet_Size VARCHAR(20),
    Outlet_Location_Type VARCHAR(20),
    Outlet_Type VARCHAR(50)
);
